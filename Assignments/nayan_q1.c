#include <stdio.h>
int main(){ int n;
char string[n]=0;
printf("Enter size of string");
scanf("%d", &n);
printf("Enter char of string");
for(int i=0; i<=n; i++){
    scanf("%c", &string[i]);}
for (int i=0; i<=n; i++):
    printf("%d", string[i]);

for (int i=0; i<=n; i++){
    if (a[i+1]>a[i]){
char max_lnf = a[i+1];
        }
    else {
char max_lnf = a[i];
        }
}
int c=0;
if (max_lnf == ')' || max_lnf == '('){
        c+=1;
    }
else {
        printf("Wrong data");
    }
printf("length of longest valid substring is %d", c);
}




















